version: "3.9"

services:
  frontend:
    pod: arsip-pod
    image: arsip-web:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: arsip-web
    env_file:
      - .env
    ports:
      - "3000:3000"
    restart: unless-stopped
